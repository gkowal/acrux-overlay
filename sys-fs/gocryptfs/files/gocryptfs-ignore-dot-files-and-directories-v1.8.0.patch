diff -uNr gocryptfs-1.8.0-orig/internal/fusefrontend/fs_dir.go gocryptfs-1.8.0/internal/fusefrontend/fs_dir.go
--- gocryptfs-1.8.0-orig/internal/fusefrontend/fs_dir.go	2020-05-09 09:20:21.000000000 -0300
+++ gocryptfs-1.8.0/internal/fusefrontend/fs_dir.go	2020-05-09 23:40:16.758960055 -0300
@@ -297,6 +297,10 @@
 	// Filter and decrypt filenames
 	for i := range cipherEntries {
 		cName := cipherEntries[i].Name
+		if cName[0] == '.' {
+			// silently ignore dot files and directories
+			continue
+		}
 		if dirName == "" && cName == configfile.ConfDefaultName {
 			// silently ignore "gocryptfs.conf" in the top level dir
 			continue
